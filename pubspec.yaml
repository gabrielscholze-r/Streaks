name: streaks
description: An application to track and manage habits.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  get_it: ^7.2.0

  provider: ^6.0.5
  flutter_local_notifications: ^17.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3
  equatable: ^2.0.5
  uuid: ^4.4.0
  permission_handler: ^11.3.1
  timezone: ^0.9.4


dev_dependencies:
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.5.4
  flutter_test:
    sdk: flutter
  test: ^1.24.9
flutter:
  uses-material-design: true